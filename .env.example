# Jarvis — Environment Variables
# Copy this file to .env and fill in the values.

# Groq API key (free tier) — get one at https://console.groq.com/keys
# Not needed until build step 7 (Groq fallback). Leave blank until then.
GROQ_API_KEY=
